package com.codewithshiva.retailpos.audit;

/**
 * Enum representing the types of entities that can be audited.
 */
public enum EntityType {
    USER,
    PRODUCT,
    VARIANT,
    SALE,
    PURCHASE,
    SUPPLIER,
    STOCK_ADJUSTMENT,
    SETTINGS
}
